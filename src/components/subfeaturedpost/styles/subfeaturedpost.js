import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    flexGrow: 1,
  },
  card: {
      display: 'flex',
  },
  cardDetails: {
      flex: 2,
    },
  cardMedia: {
      width: 160,
    },
  });